import type { EnumsIcons } from '@proedis/modeler';


/* --------
 * Exports
 * -------- */
const sharedObjectsIcons: EnumsIcons = {
<% sharedObjects.forEach(function (s) { %>
  <%- s.name %>: {
  <% s.values.forEach(function (value) { %>
    <%= value %>: 'bug',<% }) %>
  },
<% }) %>
};

export default sharedObjectsIcons;
