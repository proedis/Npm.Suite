import { createStyles } from '@mantine/core';

import { TABLE_REFS } from './Table.constants';


/* --------
 * Style Definition
 * -------- */
export default createStyles({
  root: {
    ref: TABLE_REFS.HEADER_CELL
  }
});
